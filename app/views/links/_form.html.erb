<%= form_for(link) do |f| %>
  <% if link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :field_id %>
    <%= f.number_field :field_id %>
  </div>

  <div class="field">
    <%= f.label :connector_id %>
    <%= f.number_field :connector_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
